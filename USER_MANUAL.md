# คู่มือการใช้งานระบบ IT Stock Pro (User Manual)

ระบบ **IT Stock Pro** เป็นระบบบริหารจัดการสต็อกอุปกรณ์ไอที (IT Inventory Management) ที่ออกแบบมาเพื่อควบคุมยอดคงเหลือ ติดตามประวัติการรับ-จ่าย แจ้งเตือนของใกล้หมด และช่วยอำนวยความสะดวกในการจัดซื้อ (PO/PR) อย่างเป็นระบบ

---

## 1. ระบบเข้าสู่ระบบ (Login & Authentication)
ระบบเชื่อมต่อกับระบบฐานข้อมูลพนักงานของ Daikin โดยตรง
- **เงื่อนไข/การทำงาน:** ผู้ใช้กรอก Username/Password ของรหัสพนักงาน หากรหัสถูกต้อง ระบบจะดึงข้อมูล ชื่อย่อ (ShortName), แผนก (SECT_Short) และสิทธิ์การใช้งานมาบันทึกใน Session อัตโนมัติ ทำให้ผู้ใช้ไม่ต้องระบุชื่อตัวเองซ้ำเวลาทำรายการต่างๆ

---

## 2. หน้าหลัก (Dashboard)
ภาพรวมสรุปข้อมูลทั้งหมด เพื่อให้ทีม IT กวาดสายตาดูสถานะสต็อกได้ทันที
- **สรุปตัวเลข:** โชว์ยอดอุปกรณ์ทั้งหมด, มูลค่ารวมในสต็อก, ยอดการเบิกใช้งานในเดือนนี้ และจำนวนสินค้าที่ต่ำกว่าเกณฑ์การสั่งซื้อ (Min Stock)
- **กราฟวิเคราะห์ (Charts):** แสดงสัดส่วนมูลค่าสต็อกตามประเภท (Device Type) เป็น Stacked Bar Chart ช่วยให้รู้ว่าเงินจมอยู่กับหมวดหมู่ไหนมากที่สุด
- **Recent Transactions:** แสดงประวัติการเข้า-ออกของสต็อกล่าสุด

---

## 3. จัดการคลังสินค้า (Inventory Management)
หัวใจหลักของการค้นหา ดูยอด ตัดเบิก และแก้ไขสินค้า
- **เงื่อนไข/การทำงาน:** 
  - สามารถค้นหาสินค้าได้จากชื่อ, รหัส, ประเภท, หรือใช้ Scanner ยิงบาร์โค้ด
  - สินค้าแต่ละตัวจะมีค่า `CurrentStock` (ยอดยกมา), `MinStock` (จุดสั่งซื้อ), และ `MaxStock` (แนะนำเก็บสูงสุด)
- **การเบิกหน้าคลัง (Withdraw / Cart):**
  - คล้ายระบบตะกร้าสินค้า (Cart) สามารถกดยืนยันเบิกทีละรายการ หรือหยิบใส่ตะกร้าหลายชิ้นแล้วกดยืนยันพร้อมกัน
  - **เงื่อนไข:** ต้องระบุ **เหตุผล (Reason)** (เช่น อุปกรณ์เสีย, ให้พนักงานใหม่, ยืมชั่วคราว) และระบุยอดยืนยันการตัดจำนวน ระบบจะลบยอด `CurrentStock` ทันที และบันทึกลง History

---

## 4. ระบบสั่งซื้อ (Purchase Orders & PR Form)
จัดการเอกสารสั่งซื้อขาเข้า (PO) และสร้างใบเสนอขอซื้อ (PR)
- **สแกนใบสั่งซื้อด้วย AI (OCR):**
  - **การทำงาน:** อัปโหลดไฟล์ PDF (ใบสั่งซื้อของ Daikin) ระบบจะใช้ Tesseract AI ดึงเอาเลข PO, ชื่อ Vendor (ตัด .LTD ให้สมบูรณ์), รายการสินค้า, จำนวน, ราคา, Budget No., และผู้เบิก (Delivery To) ออกมาใส่ฟอร์มอัตโนมัติ โดยไม่ต้องพิมพ์มือ
  - **เงื่อนไข:** Vendor และรายการสินค้า หากชื่อตรงกับในฐานข้อมูล (Master) ระบบจะผูกรหัส ProductID ให้ทันที
- **พิมพ์ใบขอซื้อ (PR Form A5):**
  - สร้างเอกสาร PR ขนาด A5 ไว้ปริ้นไปแนบกับบิล
  - **ฟีเจอร์เด่น:** มีปุ่ม **"ด่วน (Min Stock)"** ที่มุมขวาบน ระบบจะเช็คว่าสินค้าไหนที่ `CurrentStock` น้อยกว่าหรือเท่ากับ `MinStock` แล้วขึ้นลิสต์ให้คลิกเพิ่มลงฟอร์มได้ทันที เมื่อกดแล้วจะไฮไลต์ติ๊กถูก (✅) ให้รู้ว่าเลือกไปแล้ว

---

## 5. การรับสินค้าเข้าคลัง (Receive / Inbound)
เมื่อสั่งของไปแล้วและซัพพลายเออร์ส่งของมาตึก ต้องมารับเข้าผ่านหน้านี้
- **การทำงาน:** 
  - เลือก PO ที่มีสถานะ Open/Pending หรือ Partial
  - กรอกเลข Invoice/DO จากซัพพลายเออร์
  - ระบุจำนวน "ที่ได้รับจริง" (Receive Qty)
  - **เงื่อนไขพิเศษ:** ระบบจะคำนวณราคาเฉลี่ยล่าสุด (Last Average Price) และบวกยอด `CurrentStock` กลับเข้าคลัง
  - หากรับของครบตามที่สั่งใน PO แล้ว ระบบจะเปลี่ยนสถานะ PO เป็น `Completed` อัตโนมัติ หากรับมาแค่บางส่วนจะเป็น `Partial`

---

## 6. ตรวจนับสต็อกรายเดือน (Stock Count / Audit)
ระบบสำหรับ Audit สต็อกประจำเดือน หรือ ปรับปรุงยอดให้ตรงกับความเป็นจริง
- **การทำงาน:**
  1. สร้าง **Session** รหัสการนับใหม่ (Draft)
  2. ยก Scanner ไปหน้าตู้ แล้วยิงบาร์โค้ด หรือพิมพ์หาชื่อ
  3. ใส่ยอด **"ที่นับได้จริง (Actual)"** ระบบจะเทียบกับยอดในระบบ (System) และโชว์ค่าความต่างผลต่าง (+/-)
  4. หากนับเสร็จแล้วกด **"บันทึกยืนยันผลการนับ (Confirm)"**
- **เงื่อนไข:** ทันทีที่กด Confirm ระบบจะเอา "ยอดที่นับได้จริง" เข้าไปทับเป็น `CurrentStock` จริงในฐานข้อมูล ลบ Session ทิ้งจากรายการนับปกติ และบันทึกรายงานเข้าสู่ "ประวัติการตรวจนับ (History)" 

---

## 7. ประวัติระบบ (History / Logs)
Audit Trail เก็บทุกความเคลื่อนไหว
- ดูย้อนหลังได้ทั้งหมด ทั้งการ นำเข้า (Receive), การเบิกจ่าย (Withdraw), และผลต่างจากการนับสต็อก (Stock Adjustments)
- ระบุคนที่ทำรายการ (Username) และเวลา Timestamp ได้ละเอียด 

---

## 8. ระบบแจ้งเตือน License และ MA (M/A & License)
แยกการจัดการสินค้าประเภท "สัญญาบริการ" หรือ "Software" ออกมาดูแลต่างหาก
- **การทำงาน:** เก็บข้อมูลวันหมดอายุ (Expiry Date) ของ Software/System แต่ละตัว
- **เงื่อนไขการแจ้งเตือน:** 
  - หากสัญญาใกล้หมดอายุ (เหลือน้อยกว่า 90 วัน) บรรทัดนั้นจะกะพริบเป็น **สีเหลือง** 
  - หากหมดอายุแล้ว (Expired) จะกะพริบเป็น **สีแดง (Blinking Alert)** ทันที เพื่อป้องกันระบบล่มจากการลืมต่อสัญญา

---

## 9. ข้อมูลเครื่องพิมพ์ / หมึก (Ink & Toner Stock)
หน้าพิเศษที่ต่อกับฐานข้อมูลโรงงานอีกตึก (`dbDCI`)
- **การทำงาน:** เป็นหน้า Dashboard แบบ Read-only โชว์ตารางเช็คยอดฝั่งโรงงานผลิตแบบ Real-time ดึงข้อมูลมาแสดงเพื่อประกอบการตัดสินใจเบิกหมึกข้ามโรงงาน

---

## 10. ระบบแจ้งเตือนและ Report (Daily Email & Line)
- ระบบ Backend มีกระบวนการที่จะเช็คยอด "สินค้าเข้าขั้นต่ำ (Min Stock)" และส่งอีเมลสวยงามหรือแจ้งเตือนรายการไปยังฝ่ายที่เกี่ยวข้อง (Automated Job)

---

### ข้อแนะนำเพิ่มเติม
1. ควรเช็คการเชื่อมต่อ API ของ Daikin อย่างสม่ำเสมอ หาก Authentication กดยืนยันแล้วเปิดเข้าสู่ระบบไม่ได้ อาจจะเกิดจาก Server ของบริษัทมีการเปลี่ยนรูปแบบโครงสร้าง Data  
2. ทุกครั้งที่มีการสร้างอุปกรณ์หรืออะไหล่ชิ้นใหม่ (Manual Add) อย่าลืมตั้งค่า `MinStock` ให้เหมาะสมกับการใช้งานจริง เพื่อให้การแจ้งเตือนสีส้ม (รีบสั่ง) ในหน้า Dashboard และหน้าจัดซื้อทำงานได้เต็มประสิทธิภาพครับ
